# ui

Shared UI component library (built on shadcn/ui + Tailwind) consumed by `apps/web`.

- Presentational, reusable components (buttons, forms, tables, dialogs, charts).
- No business logic and no direct data fetching — components receive data via props.
- Themed consistently so all PlatformTrust surfaces look and behave the same.
