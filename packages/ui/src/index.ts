/**
 * @platformtrust/ui
 *
 * Shared, accessible UI primitives for PlatformTrust apps (React 19).
 * This is a minimal boundary package — one primitive today, not a design system.
 */

export { Button, type ButtonProps } from './button.js';
