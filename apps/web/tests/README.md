# web tests

Playwright end-to-end tests and unit tests for the web app live here.

- `*.spec.ts` — Playwright browser tests.
- Run with `npm run test`.
- Cross-app end-to-end flows also live in the top-level `tests/end-to-end/`.
