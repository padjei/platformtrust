# pnpm configuration
# Enforce the pinned package manager and a reproducible, strict install.
engine-strict=true
# Keep the dependency tree strict; packages must declare what they import.
auto-install-peers=true
# Do not execute lifecycle scripts of arbitrary transitive dependencies.
# (Allowed build scripts, if any, are opted in per-project.)
