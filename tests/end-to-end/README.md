# end-to-end tests

Playwright end-to-end tests covering full user flows across the web app and API, e.g.:

- Sign in, create an audit, connect a read-only connector.
- Ingest evidence and run a deterministic readiness score.
- View and export the readiness report.

Run against a fully deployed/local stack (web + api + worker + postgres).
